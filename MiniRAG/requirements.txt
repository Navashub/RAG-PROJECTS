# MiniRAG - minimal dependencies only
# Do NOT paste output from pip freeze here
# Only list what MiniRAG actually imports

# Web server
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Core
numpy==1.26.4
requests==2.31.0

# Production embeddings (sentence-transformers runs on Render without Ollama)
sentence-transformers==2.7.0
torch==2.2.2
transformers==4.40.0